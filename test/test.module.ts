import { NgModule } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';

@NgModule({
  imports: [HttpClientModule], // Importa los módulos necesarios para tus pruebas
})
export class TestModule {}
